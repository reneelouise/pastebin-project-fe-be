CREATE DATABASE rbgdatabase;

CREATE TABLE pastes( pastes_id SERIAL PRIMARY KEY, pastes VARCHAR(1000) );


INSERT INTO pastes(pastes) VALUES ('Real change enduring change happens one step at a time.');